import { motion } from 'framer-motion';
import { Flex, Container, Heading, Avatar } from '@chakra-ui/react';
import { nameEnter } from '../animation/animation';
import avatar from '../lib/avatar.png';

function Intro() {
  const MotionHeading = motion(Heading);
  const MotionContainer = motion(Container);
  const MotionFlex = motion(Flex);

  return (
    <MotionFlex
      variants={nameEnter}
      direction="column"
      mt="20rem"
      ml="10rem"
      initial="initial"
      animate="fadeIn"
      key="menu"
    >
      <MotionHeading
        as='h1'
        fontSize="8xl"
        variants={nameEnter}
        m="0"
        lineHeight="6.5rem"
      >
        Hello, World!
        <br />
        I'm Gadyer Gonzalez.
      </MotionHeading>
      <MotionContainer
        as="i"
        fontSize="2xl"
        variants={nameEnter}
        p='0'
        m="0"
        color="gray"
      >
        Full Stack Developer
      </MotionContainer>
      <Avatar src={avatar} size='2xl' alignSelf='flex-end' position='relative' top='-10rem' right='20rem'/>
    </MotionFlex>
  );
}

export default Intro;
